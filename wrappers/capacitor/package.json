{
  "name": "lyceum-android",
  "private": true,
  "version": "0.1.0",
  "description": "Lyceum reader — Capacitor (Android) shell wrapping the web SPA (LYCM-300).",
  "type": "module",
  "scripts": {
    "copy-web": "node copy-web.mjs",
    "add:android": "cap add android && node apply-android-overrides.mjs",
    "sync": "npm run copy-web && cap sync android && node apply-android-overrides.mjs",
    "open": "cap open android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "typescript": "~5.4.0"
  }
}
